{#
  Template: preview.md.j2
  Description: Generates a preview of table rows in JSONL format

  Available context:
    - table_name (str): Name of the table
    - dataset (str): Schema/dataset name
    - db (DatabaseContext): Database context with helper methods
#}
{% set rows = db.preview() %}
# {{ table_name }} - Preview

**Dataset:** `{{ dataset }}`

## Rows ({{ rows | length }})

{% for row in rows %}
- {{ row | to_json }}
{% endfor %}

---
*Generated by nao sync with custom template (in example/templates/databases/preview.md.j2)*
