# Copy to .env and fill in real values to run integration tests.
# The .env file is gitignored — never commit credentials.
# When a provider's env vars are not set, that provider's integration tests are skipped.
#
# Defaults below for Postgres, ClickHouse, and MSSQL match docker-compose.test.yml.
# Run: docker compose -f docker-compose.test.yml up -d

# Postgres (tests skipped when POSTGRES_HOST is not set)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=nao
POSTGRES_PASSWORD=nao
POSTGRES_DATABASE=postgres
POSTGRES_SCHEMA=public

# ClickHouse (tests skipped when CLICKHOUSE_HOST is not set)
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
# Port for the native TCP protocol (port 9000 by default, exposed by docker-compose.test.yml).
CLICKHOUSE_NATIVE_PORT=9000
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=admin
CLICKHOUSE_SECURE=false

# MSSQL (tests skipped when MSSQL_HOST is not set)
MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=NaoTest1!
MSSQL_SCHEMA=dbo
# On macOS with Homebrew ODBC: MSSQL_DRIVER=/opt/homebrew/lib/libtdsodbc.so

# Redshift
REDSHIFT_HOST=your-cluster.region.redshift.amazonaws.com
REDSHIFT_PORT=5439
REDSHIFT_DATABASE=dev
REDSHIFT_USER=awsuser
REDSHIFT_PASSWORD=changeme
REDSHIFT_SCHEMA=public
REDSHIFT_SSLMODE=require

# Trino
TRINO_HOST=localhost
TRINO_PORT=8080
TRINO_CATALOG=hive
TRINO_USER=nao
TRINO_PASSWORD=

# StarRocks (tests skipped when STARROCKS_HOST is not set)
STARROCKS_HOST=localhost
STARROCKS_PORT=9030
STARROCKS_USER=root
STARROCKS_PASSWORD=
